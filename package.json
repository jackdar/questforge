{
  "name": "questforge",
  "private": true,
  "type": "module",
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "npm run db:dev && (npm run dev -w server & npm run dev -w client)",
    "test": "npm run db:dev && npm test --workspaces --if-present",
    "db:dev": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d --wait db"
  }
}
